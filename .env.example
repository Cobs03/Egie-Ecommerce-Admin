# Environment Variables for Egie E-commerce Admin Panel

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual Supabase credentials
# 3. NEVER commit the .env file to Git
# 4. Get your credentials from: https://app.supabase.com/project/YOUR_PROJECT/settings/api

# Security Notes:
# - VITE_SUPABASE_ANON_KEY is safe to expose in browser (public key)
# - It's protected by Row Level Security (RLS) policies in Supabase
# - Service Role Key should NEVER be in frontend code
# - Keep .env file in .gitignore at all times
